[tool.poetry]
name = "goit-python-web-hw02"
version = "0.1.0"
description = "CLI бот 'Персональний помічник'"
authors = ["Сергій <your-email@example.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.13"

[tool.poetry.group.dev.dependencies]
pytest = "^7.0"

[tool.poetry.scripts]
assistant = "main:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"